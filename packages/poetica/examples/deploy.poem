# Deploy workflow — Level 4 (external)
name deploy_service

seed version with "1.2.0"
seed env with "production"
grow artifact with version
pack artifact as docker
lift artifact to registry
use healthcheck(url: "https://api.example.com", timeout: 30)
emit "deploy" version
remember last_deploy: version
bloom "deployed"
